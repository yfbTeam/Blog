<%- include('../common/header') -%>
<%- include('../common/nav') -%>
<aside class="right-side">
    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="panel">
                    <header class="panel-heading">
                        推荐属性列表
                    </header>
                    <div class="panel-body table-responsive">
                        <div class="box-tools m-b-15 clearfix">
                           <a type="button" class="btn btn-primary pull-right" href="/admin/addAttribute">添加推荐属性</a>
                           <a type="button" class="btn btn-primary pull-right" href="/admin/attributeList/1">推荐属性回收站</a>
                        </div>
                        <table class="table table-hover">
                            <tbody>
                                <tr>
                                    <th>序号</th>
                                    <th>推荐属性名称</th>
                                    <th>操作</th>
                                </tr>
                                <% for(var i = 0;i < attributeList.length;i++){ %>
                                <tr>
                                    <td><%= (i+1) %></td>
                                    <td><%= attributeList[i].attributename %></td>
                                    <td>
                                        <a href="/admin/editAttribute/<%= attributeList[i]._id %>">修改</a>
                                        <a href="/admin/deleteAttribute/<%= attributeList[i]._id%>">删除</a>
                                    </td>
                                </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
        </div>
    </section>
</aside>
<%- include('../common/footer') -%>